---
const { class: className = "", ...rest } = Astro.props;
---
<div
  class={`flex flex-col border p-5 rounded-sm items-center transition-all hover:scale-110 hover:border-white duration-300 ${className}`}
  {...rest}
>
  <slot />
</div>